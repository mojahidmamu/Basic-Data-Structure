#include <bits/stdc++.h>
using namespace std;
int main()
{
    list<int> l(100);
    list<int> v(100, 10);
    list<char> v(50, 'a');
    list<float> v(30, 2.5);
    list<long long> v(40, 100);
    cout << l.size() << endl;
    return 0;
}