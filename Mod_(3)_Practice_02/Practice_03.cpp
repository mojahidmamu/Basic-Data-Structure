// Search Query:
#include <bits/stdc++.h>
using namespace std;
int main()
{
    int N;
    cin >> N;
    int arr[N];
    for (int i = 0; i < N; i++)
    {
        cin >> arr[i];
    }
    int Q;
    cin >> Q;
    while (Q--)
    {
        int X;
        cin >> X;
    }

    return 0;
}
