#include <bits/stdc++.h>
using namespace std;
int main()
{
    int TestCase;
    cin >> TestCase;
    while (TestCase--)
    {
        int commandNumber;
        cin >> commandNumber;
        if (commandNumber == 0)
        {
            int personNmae;
            cin >> personNmae;
        }
        else
        {
            cout << "Invalid" << endl;
        }
    }
    return 0;
}
