#include <bits/stdc++.h>
using namespace std;
int main()
{
    int TestCase;
    cin >> TestCase;
    while (TestCase--)
    {
        int commandNumber;
        cin >> commandNumber;
        while (commandNumber == 0)
        {
            int personNmae;
            cin >> personNmae;
        }
        
    }
    return 0;
}
